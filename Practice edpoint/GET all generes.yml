info:
  name: GET all generes
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/genres
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
