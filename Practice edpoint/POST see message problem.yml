info:
  name: POST see message problem
  type: http
  seq: 13

http:
  method: POST
  url: http://localhost:3000/book
  body:
    type: json
    data: |-
      {
        "rating": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
