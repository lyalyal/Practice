info:
  name: PATCH rename book
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:3000/book/1776171621386
  body:
    type: json
    data: |-
      {
        "title": "Сяйво",
        "status": "done"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
