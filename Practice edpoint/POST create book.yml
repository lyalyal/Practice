info:
  name: POST create book
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/book
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
